<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.polaris.platform.mapper.PlatformDatasourceVersionMapper">

    <resultMap type="com.polaris.platform.domain.PlatformDatasourceVersion" id="PlatformDatasourceVersionResult">
        <id property="id" column="id" />
        <result property="tenantId" column="tenant_id" />
        <result property="datasourceId" column="datasource_id" />
        <result property="versionNo" column="version_no" />
        <result property="host" column="host" />
        <result property="port" column="port" />
        <result property="databaseName" column="database_name" />
        <result property="username" column="username" />
        <result property="password" column="password_cipher" />
        <result property="sslEnabled" column="ssl_enabled" />
        <result property="connectTimeoutSeconds" column="connect_timeout_seconds" />
        <result property="queryTimeoutSeconds" column="query_timeout_seconds" />
        <result property="verificationStatus" column="verification_status" />
        <result property="databaseProductName" column="database_product_name" />
        <result property="databaseProductVersion" column="database_product_version" />
        <result property="lastTestTime" column="last_test_time" />
        <result property="lastTestLatencyMs" column="last_test_latency_ms" />
        <result property="lastTestErrorCode" column="last_test_error_code" />
        <result property="createBy" column="create_by" />
        <result property="createTime" column="create_time" />
    </resultMap>

    <sql id="selectDatasourceVersionVo">
        select id, tenant_id, datasource_id, version_no, host, port, database_name,
               username, password_cipher, ssl_enabled, connect_timeout_seconds,
               query_timeout_seconds, verification_status, database_product_name,
               database_product_version, last_test_time, last_test_latency_ms,
               last_test_error_code, create_by, create_time
        from platform_datasource_version
    </sql>

    <select id="selectById" resultMap="PlatformDatasourceVersionResult">
        <include refid="selectDatasourceVersionVo"/>
        where id = #{id}
    </select>

    <select id="selectByDatasourceAndVersion" resultMap="PlatformDatasourceVersionResult">
        <include refid="selectDatasourceVersionVo"/>
        where datasource_id = #{datasourceId} and version_no = #{versionNo}
        limit 1
    </select>

    <select id="selectByDatasourceId" resultMap="PlatformDatasourceVersionResult">
        <include refid="selectDatasourceVersionVo"/>
        where datasource_id = #{datasourceId}
        order by version_no desc
    </select>

    <select id="selectNextVersionNo" resultType="int">
        select coalesce(max(version_no), 0) + 1
        from platform_datasource_version
        where datasource_id = #{datasourceId}
    </select>

    <insert id="insert" parameterType="com.polaris.platform.domain.PlatformDatasourceVersion" useGeneratedKeys="true" keyProperty="id">
        insert into platform_datasource_version(
            tenant_id, datasource_id, version_no, host, port, database_name, username,
            password_cipher, ssl_enabled, connect_timeout_seconds, query_timeout_seconds,
            verification_status, database_product_name, database_product_version,
            last_test_time, last_test_latency_ms, last_test_error_code, create_by, create_time
        ) values (
            #{tenantId}, #{datasourceId}, #{versionNo}, #{host}, #{port}, #{databaseName},
            #{username}, #{password}, #{sslEnabled}, #{connectTimeoutSeconds},
            #{queryTimeoutSeconds}, #{verificationStatus}, #{databaseProductName},
            #{databaseProductVersion}, #{lastTestTime}, #{lastTestLatencyMs},
            #{lastTestErrorCode}, #{createBy}, sysdate()
        )
    </insert>
</mapper>
