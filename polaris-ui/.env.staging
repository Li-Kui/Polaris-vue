# 页面标题
VUE_APP_TITLE = AI管理系统

BABEL_ENV = production

NODE_ENV = production

# 测试环境配置
ENV = 'staging'

# AI管理系统/测试环境
VUE_APP_BASE_API = '/stage-api'

# 默认大模型配置
VUE_APP_DEFAULT_MODEL = 'deepseek-chat'

